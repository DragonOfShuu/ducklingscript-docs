---
title: GUI/WINDOWS/META
sidebar_position: 15
description: The GUI/WINDOWS/META command.
---

# GUI

`GUI`/`WINDOWS`/`META` emulates an input as if the user had pressed the windows/meta key and the `<chr>` if given.

**Syntax:** `GUI <optional chr>` or `WINDOWS <optional chr>` or `META <optional chr>`

Example:
```
GUI
GUI a
```
Output
```
Pressed WINDOWS/META
Pressed WINDOWS/META+a
```